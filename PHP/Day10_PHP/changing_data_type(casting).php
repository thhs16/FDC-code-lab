<?php

    // (string)
    // (int)
    // (float)
    // (bool)
    // (array)
    // (object)
    // (unset) // converts to data type NULL
    
    $num = 10;

    var_dump($num); //int(10) // includes data type, doesn't need keyword typeof like in JS  